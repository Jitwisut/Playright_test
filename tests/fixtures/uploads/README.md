# Upload fixtures

Upload bytes are generated per test in `test-results/` so the repository contains no executable or personal files. The upload route is blocked by the registration fixture; tests never send these synthetic files to the public upload endpoint.
